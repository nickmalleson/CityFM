# Pins verified working on macOS arm64 (Apple Silicon), Python 3.9, 2026-09-18.
# Differences from requirements.txt:
#   - rasterio / scikit-learn relaxed: 1.3.2 / 0.24.2 have no arm64 wheels
#   - torchvision added: needed to unpickle the pre-trained model.pkl files
#   - setuptools<70 added: torchmetrics 0.11.3 imports pkg_resources
#   - gdown added: downloads the pre-trained models from Google Drive
# Install with:  uv venv --python 3.9 .venv && uv pip install --only-binary :all: -r requirements-arm64.txt
matplotlib==3.6.2
networkx==2.8.8
numpy==1.23.1
osmnx==1.2.2
pandas==1.5.1
pyproj==3.4.0
rasterio>=1.3.3,<1.4
Requests==2.31.0
scikit_learn>=1.0,<1.4
Shapely==1.8.5
torch==1.13.1
torchvision==0.14.1
torchmetrics==0.11.3
transformers==4.26.1
setuptools<70
gdown
